apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "advisoryhub.fullname" . }}
  labels:
    {{- include "advisoryhub.labels" . | nindent 4 }}
data:
  {{- include "advisoryhub.envMap" . | nindent 2 }}
